# Consumer ProGuard rules for :core-network.
# kotlinx.serialization and Retrofit already ship their own consumer rules inside
# their AARs, so nothing app-specific is required here today. Kept as an explicit,
# versioned file (it was referenced but missing from the original :data module)
# so future serializable models/interceptors have an obvious place to add rules.
